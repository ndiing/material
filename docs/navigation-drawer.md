# MDNavigationBarComponent

The `MDNavigationBarComponent` interface represents a `<md-navigation-drawer>` element in the DOM. Represents a navigation drawer component that includes a tree view for navigation.

## Inheritance
`MDSheetComponent`

