# MDSideSheetComponent
The `MDSideSheetComponent` interface represents a `<md-side-sheet>` element in the DOM. Represents a side sheet component that extends MDSheetComponent.

## Inheritance
`MDSheetComponent`





