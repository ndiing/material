# toPascalCase

Converts a string to PascalCase.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to PascalCase. |

# toCamelCase

Converts a string to camelCase.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to camelCase. |

# toSnakeCase

Converts a string to snake_case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to snake_case. |

# toKebabCase

Converts a string to kebab-case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to kebab-case. |

# toFlatCase

Converts a string to flatcase.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to flatcase. |

# toUpperFlatCase

Converts a string to UPPERFLATCASE.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to UPPERFLATCASE. |

# toPascalSnakeCase

Converts a string to Pascal_Snake_Case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to Pascal_Snake_Case. |

# toCamelSnakeCase

Converts a string to camel_Snake_Case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to camel_Snake_Case. |

# toScreamingSnakeCase

Converts a string to SCREAMING_SNAKE_CASE.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to SCREAMING_SNAKE_CASE. |

# toTrainCase

Converts a string to Train-Case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to Train-Case. |

# toCobolCase

Converts a string to COBOL-CASE.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to COBOL-CASE. |

# toTitleCase

Converts a string to Title Case.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `string` | (string) | The input string to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The string converted to Title Case. |

# stringifyDatetimeLocal

Converts a date object to a string formatted as YYYY-MM-DDTHH:MM.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The date formatted as YYYY-MM-DDTHH:MM. |

# stringifyDate

Converts a date object to a string formatted as YYYY-MM-DD.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The date formatted as YYYY-MM-DD. |

# stringifyYear

Converts a date object to a string formatted as YYYY.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The date formatted as YYYY. |

# stringifyMonth

Converts a date object to a string formatted as YYYY-MM.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The date formatted as YYYY-MM. |

# stringifyTime

Converts a date object to a string formatted as HH:MM.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The time formatted as HH:MM. |

# stringifyWeek

Converts a date object to a string formatted as YYYY-WW.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `date` | (Date) | The date object to convert. |

#### Returns

| Type | Description |
|------|-------------|
| string | The date formatted as YYYY-WW. |

# parseDatetimeLocal

Parses a datetime-local string into a Date object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `datetimeLocal` | (string) | The datetime-local string to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The parsed Date object. |

# parseDate

Parses a date string (YYYY-MM-DD) into a Date object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `dateString` | (string) | The date string (YYYY-MM-DD) to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The parsed Date object. |

# parseYear

Parses a year string (YYYY) into a Date object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `yearString` | (string) | The year string (YYYY) to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The parsed Date object. |

# parseMonth

Parses a month string (YYYY-MM) into a Date object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `monthString` | (string) | The month string (YYYY-MM) to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The parsed Date object. |

# parseTime

Parses a time string (HH:MM) into a Date object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `timeString` | (string) | The time string (HH:MM) to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The parsed Date object. |

# parseWeek

Parses a week string (YYYY-WW) into a Date object representing the start of that week.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `weekStr` | (string) | The week string (YYYY-WW) to parse. |

#### Returns

| Type | Description |
|------|-------------|
| Date | The Date object representing the start of the parsed week. |

# isDefined

Checks if a value is defined and not null.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `value` | (*) | The value to check. |

#### Returns

| Type | Description |
|------|-------------|
| boolean | Returns true if the value is defined and not null, otherwise false. |

# createQueue

Creates a queue function that executes callbacks asynchronously in sequence.






#### Returns

| Type | Description |
|------|-------------|
| function | A function that when called with a callback function, queues the callback for execution. |

# hexToRgba

Converts a hexadecimal color representation to RGBA components.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `hex` | (string) | The hexadecimal color string (e.g., &quot;#RRGGBB&quot; or &quot;#RRGGBBAA&quot;). |

#### Returns

| Type | Description |
|------|-------------|
| Object | The RGBA components. |

# hexToHsla

Converts a hexadecimal color representation to HSLA components.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `hex` | (string) | The hexadecimal color string (e.g., &quot;#RRGGBB&quot; or &quot;#RRGGBBAA&quot;). |

#### Returns

| Type | Description |
|------|-------------|
| Object | The HSLA components. |

# hslaToRgba

Converts HSLA components to RGBA components.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `h` | (number) | The hue value (0-360). |
| `s` | (number) | The saturation value (0-1). |
| `l` | (number) | The lightness value (0-1). |
| `a` | (number) | The alpha value (0-1). |

#### Returns

| Type | Description |
|------|-------------|
| Object | The RGBA components. |

# rgbaToHex

Converts RGBA components to a hexadecimal color representation.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `r` | (number) | The red value (0-255). |
| `g` | (number) | The green value (0-255). |
| `b` | (number) | The blue value (0-255). |
| `a` | (number) | The alpha value (0-1). |

#### Returns

| Type | Description |
|------|-------------|
| string | The hexadecimal color string (e.g., &quot;#RRGGBB&quot; or &quot;#RRGGBBAA&quot;). |

# hslaToHex

Converts HSLA components to a hexadecimal color representation.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `h` | (number) | The hue value (0-360). |
| `s` | (number) | The saturation value (0-1). |
| `l` | (number) | The lightness value (0-1). |
| `a` | (number) | The alpha value (0-1). |

#### Returns

| Type | Description |
|------|-------------|
| string | The hexadecimal color string (e.g., &quot;#RRGGBB&quot; or &quot;#RRGGBBAA&quot;). |

# rgbaToHsla

Converts RGBA components to HSLA components.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `r` | (number) | The red value (0-255). |
| `g` | (number) | The green value (0-255). |
| `b` | (number) | The blue value (0-255). |
| `a` | (number) | The alpha value (0-1). |

#### Returns

| Type | Description |
|------|-------------|
| Object | The HSLA components. |

# isArrayString

Checks if a string represents an array in a stringified format.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `str` | (string) | The string to check. |

#### Returns

| Type | Description |
|------|-------------|
| boolean | True if the string represents an array, false otherwise. |

# calcPercentage

Calculates the percentage of a value relative to a range defined by min and max values.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `min` | (number) | The minimum value of the range. |
| `max` | (number) | The maximum value of the range. |
| `value` | (number) | The value to calculate the percentage for. |

#### Returns

| Type | Description |
|------|-------------|
| number | The percentage value (between 0 and 100). |

# calcDecimal

Calculates the decimal value representing the position of a value within a range defined by min and max values.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `min` | (number) | The minimum value of the range. |
| `max` | (number) | The maximum value of the range. |
| `value` | (number) | The value to calculate the decimal for. |

#### Returns

| Type | Description |
|------|-------------|
| number | The decimal value (between 0 and 1). |

# isValidHexColor

Checks if the provided color string is a valid hexadecimal color.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `color` | (String) | The color string to validate. |

#### Returns

| Type | Description |
|------|-------------|
| Boolean | - True if the color string is a valid hexadecimal color, false otherwise. |

# isObject

Checks if a value is an object.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `obj` | (*) | The value to check. |

#### Returns

| Type | Description |
|------|-------------|
| boolean | True if the value is an object, otherwise false. |

# setWeek

Sets the week number of the date.





#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `week` | (number) | The week number to set (between 1 and 53). |

#### Returns

| Type | Description |
|------|-------------|
| Date | The modified Date object with the week set. |

# getWeek

Gets the ISO week number of the date.






#### Returns

| Type | Description |
|------|-------------|
| number | The ISO week number of the date (1 to 53). |

