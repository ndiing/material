## Instance Methods
| Name | Parameters | Description |
| --- | --- | --- |
| toPascalCase | `string` | Converts a string to PascalCase. |
| toCamelCase | `string` | Converts a string to camelCase. |
| toSnakeCase | `string` | Converts a string to snake_case. |
| toKebabCase | `string` | Converts a string to kebab-case. |
| toFlatCase | `string` | Converts a string to flatcase. |
| toUpperFlatCase | `string` | Converts a string to UPPER_FLATCASE. |
| toPascalSnakeCase | `string` | Converts a string to Pascal_Snake_Case. |
| toCamelSnakeCase | `string` | Converts a string to camel_Snake_case. |
| toScreamingSnakeCase | `string` | Converts a string to SCREAMING_SNAKE_CASE. |
| toTrainCase | `string` | Converts a string to Train-Case. |
| toCobolCase | `string` | Converts a string to COBOL-CASE. |
| toTitleCase | `string` | Converts a string to Title Case. |
| stringifyDatetimeLocal | `date` | Converts a Date object to a string in datetime-local format (YYYY-MM-DDTHH:MM). |
| stringifyDate | `date` | Converts a Date object to a string in date format (YYYY-MM-DD). |
| stringifyYear | `date` | Converts a Date object to a string in year format (YYYY). |
| stringifyMonth | `date` | Converts a Date object to a string in month format (YYYY-MM). |
| stringifyTime | `date` | Converts a Date object to a string in time format (HH:MM). |
| stringifyWeek | `date` | Converts a Date object to a string in ISO week format (YYYY-Www). |
| parseDatetimeLocal | `datetimeLocal` | Parses a datetime-local string into a Date object. |
| parseDate | `dateString` | Parses a date string (YYYY-MM-DD) into a Date object. |
| parseYear | `yearString` | Parses a year string (YYYY) into a Date object. |
| parseMonth | `monthString` | Parses a month string (YYYY-MM) into a Date object. |
| parseTime | `timeString` | Parses a time string (HH:MM) into a Date object. |
| parseWeek | `weekStr` | Parses an ISO week string (YYYY-WW) into a Date object representing the start of the week. |
| hexToRgba | `hex` | Converts a hexadecimal color string to RGBA object. |
| hexToHsla | `hex` | Converts a hexadecimal color string to HSLA object. |
| hslaToRgba | `h`, `s`, `l`, `a` | Converts HSLA values to RGBA object. |
| rgbaToHex | `r`, `g`, `b`, `a` | Converts RGBA values to hexadecimal color string. |
| hslaToHex | `h`, `s`, `l`, `a` | Converts HSLA values to hexadecimal color string. |
| rgbaToHsla | `r`, `g`, `b`, `a` | Converts RGBA values to HSLA object. |
| isValidHexColor | `color` | Checks if a string is a valid hexadecimal color string. |
| isObject | `obj` | Checks if a value is an object. |
| isArrayString | `str` | Checks if a string represents an array literal. |
| isDefined | `value` | Checks if a value is defined and not null. |
| calcPercentage | `min`, `max`, `value` | Calculates the percentage value relative to a given range. |
| calcDecimal | `min`, `max`, `value` | Calculates the decimal value relative to a given range. |
| createQueue |  | Creates a queue function that executes callbacks sequentially. |

