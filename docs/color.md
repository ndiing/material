## Instance Methods
| Name | Parameters | Description |
| --- | --- | --- |
| setTheme | `colorOrImage`, `customColors` | Sets the theme of the application based on either a color or an image. If a color is provided, it generates a theme using color utilities. if an image is provided, it asynchronously generates a theme from the image. |

